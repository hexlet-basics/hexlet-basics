DEFAULTS: &DEFAULTS
  slug: $LABEL
  name: $LABEL
  body: $LABEL
  state: published
  creator: full

from-full:
  <<: *DEFAULTS
  locale: ru

from-full-ru:
  <<: *DEFAULTS
  locale: ru

from-full-en:
  <<: *DEFAULTS
  locale: en
