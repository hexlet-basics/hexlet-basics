DEFAULTS: &DEFAULTS
  slug: slug_$LABEL

one:
  <<: *DEFAULTS
  language: one
  version: one

two:
  <<: *DEFAULTS
  language: two
  version: two

variables_one:
  slug: variables
  language: two
  version: variables_one

