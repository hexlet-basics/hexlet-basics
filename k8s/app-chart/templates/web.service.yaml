---

apiVersion: v1
kind: Service
metadata:
  name: "{{.Release.Name }}-web-service"
  namespace: "{{ .Release.Namespace }}"
spec:
  selector:
    app.kubernetes.io/name: "{{ .Release.Name }}-web-pod"
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
