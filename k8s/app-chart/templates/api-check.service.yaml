---

apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-api-check-service"
  namespace: "{{ .Release.Namespace }}"
spec:
  selector:
    app.kubernetes.io/name: "{{ .Release.Name }}-api-check-pod"
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
