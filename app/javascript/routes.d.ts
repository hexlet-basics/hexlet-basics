/**
 * @file Generated by js-routes 2.4.1. Based on Rails 8.1.3 routes of HexletBasics::Application.
 * @version d0f6bcca863c7a7a3f4735044f0679a4a1881447e7752d33e81b349e104f757d
 * @see https://github.com/railsware/js-routes
 */
type Optional<T> = {
    [P in keyof T]?: T[P] | null;
};
export type Collection<T> = Record<string, T>;
type BaseRouteParameter = string | boolean | Date | number | bigint;
type MethodRouteParameter = BaseRouteParameter | (() => BaseRouteParameter);
type ModelRouteParameter = {
    id: MethodRouteParameter;
} | {
    to_param: MethodRouteParameter;
} | {
    toParam: MethodRouteParameter;
};
type RequiredRouteParameter = BaseRouteParameter | ModelRouteParameter;
type OptionalRouteParameter = undefined | null | RequiredRouteParameter;
type QueryRouteParameter = OptionalRouteParameter | QueryRouteParameter[] | {
    [k: string]: QueryRouteParameter;
};
export type RouteParameters = Collection<QueryRouteParameter>;
type Serializable = Collection<unknown>;
export type Serializer = (value: Serializable) => string;
type RouteHelperExtras = {
    requiredParams(): string[];
    toString(): string;
};
type RequiredParameters<T extends number> = T extends 1 ? [RequiredRouteParameter] : T extends 2 ? [RequiredRouteParameter, RequiredRouteParameter] : T extends 3 ? [RequiredRouteParameter, RequiredRouteParameter, RequiredRouteParameter] : T extends 4 ? [
    RequiredRouteParameter,
    RequiredRouteParameter,
    RequiredRouteParameter,
    RequiredRouteParameter
] : RequiredRouteParameter[];
type RouteHelperOptions = RouteOptions & Collection<OptionalRouteParameter>;
export type RouteHelper<T extends number = number> = ((...args: [...RequiredParameters<T>, RouteHelperOptions]) => string) & RouteHelperExtras;
type Configuration = {
    prefix: string;
    default_url_options: RouteParameters;
    special_options_key: string;
    serializer: Serializer;
    deprecated_false_parameter_behavior: boolean;
    deprecated_nil_query_parameter_behavior: boolean;
    include_undefined_query_parameters: boolean;
};
export interface RouterExposedMethods {
    config(): Configuration;
    configure(arg: Partial<Configuration>): Configuration;
    serialize: Serializer;
    __route(...args: unknown[]): RouteHelper;
}
export interface RouterConstructor {
    new (config?: Partial<Configuration>): RouterExposedMethods;
}
type KeywordUrlOptions = Optional<{
    host: string;
    protocol: string;
    subdomain: string;
    port: string | number;
    anchor: string;
    trailing_slash: boolean;
    script_name: string;
    params: RouteParameters;
}>;
type RouteOptions = KeywordUrlOptions & RouteParameters;
export {};
export const configure: RouterExposedMethods['configure'];

export const config: RouterExposedMethods['config'];

export const serialize: RouterExposedMethods['serialize'];

export const __route: RouterExposedMethods['__route'];

/**
 * Generates rails route to
 * (/:suffix)/account/passkeys/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const account_passkey_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/account/passkeys
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const account_passkeys_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/account/profile
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const account_profile_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/api/language_categories/:language_category_id/qna_items/:id
 * @param {any} language_category_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_api_language_category_qna_item_path: ((
  language_category_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/api/language_categories/:language_category_id/qna_items
 * @param {any} language_category_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_api_language_category_qna_items_path: ((
  language_category_id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/api/language_landing_pages/:language_landing_page_id/qna_items/:id
 * @param {any} language_landing_page_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_api_language_landing_page_qna_item_path: ((
  language_landing_page_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/api/language_landing_pages/:language_landing_page_id/qna_items
 * @param {any} language_landing_page_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_api_language_landing_page_qna_items_path: ((
  language_landing_page_id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/api/users/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_api_user_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/api/users
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_api_users_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/banners/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_banner_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/banners
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_banners_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/blog_posts/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_blog_post_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/blog_posts
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_blog_posts_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/languages/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_language_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_categories
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_language_categories_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_categories/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_language_category_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_landing_pages/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_language_landing_page_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_landing_pages
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_language_landing_pages_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_lesson_members
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_language_lesson_members_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_lesson_reviews
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_language_lesson_reviews_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_lessons
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_language_lessons_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/languages/:language_id/versions
 * @param {any} language_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_language_versions_path: ((
  language_id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/languages
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_languages_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/leads
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_leads_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/roles/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_management_role_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/role_permissions/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_management_role_permission_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/roles
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_management_roles_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/staff_members/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_management_staff_member_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/staff_members
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_management_staff_members_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/users/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_management_user_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/users
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_management_users_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/messages
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_messages_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/reviews/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_review_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/reviews
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_reviews_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const admin_root_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /ahoy
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const ahoy_engine_path: ((
  options?: RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /ahoy/events(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const ahoy_engine_events_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /ahoy/visits(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const ahoy_engine_visits_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/ai/lessons/:lesson_id/messages(.:format)
 * @param {any} lesson_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const ai_lesson_messages_path: ((
  lesson_id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter, format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/api/blog_posts/:id(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const api_blog_post_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter, format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/blog_posts/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const blog_post_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/blog_posts/:blog_post_id/likes
 * @param {any} blog_post_id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const blog_post_likes_path: ((
  blog_post_id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/blog_posts
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const blog_posts_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/book
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const book_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/cases
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const cases_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/api/lessons/:id/check(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const check_api_lesson_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter, format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/phone_auth/confirm
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const confirm_phone_auth_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/book/create_request
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const create_request_book_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/book/download
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const download_book_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/account/profile/edit
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_account_profile_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/api/users/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_admin_api_user_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/banners/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_admin_banner_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/blog_posts/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_admin_blog_post_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/languages/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_admin_language_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_categories/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_admin_language_category_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_landing_pages/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_admin_language_landing_page_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/roles/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_admin_management_role_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/staff_members/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_admin_management_staff_member_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/users/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_admin_management_user_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/reviews/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_admin_review_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/book/edit
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_book_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/leads/:id/edit
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_lead_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/password/:token/edit
 * @param {any} token
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const edit_password_path: ((
  token: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/cases/for_teachers
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const for_teachers_cases_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/languages/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const language_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/language_categories
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const language_categories_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/language_categories/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const language_category_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/languages/:language_id/lessons/:id
 * @param {any} language_id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const language_lesson_path: ((
  language_id: RequiredRouteParameter,
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/languages
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const languages_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/leads/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const lead_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/leads
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const leads_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/magic_links/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const magic_link_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/magic_links
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const magic_links_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/map
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const map_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/my
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const my_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/account/passkeys/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_account_passkey_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/api/users/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_admin_api_user_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/banners/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_admin_banner_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/blog_posts/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_admin_blog_post_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/languages/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_admin_language_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_categories/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_admin_language_category_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_landing_pages/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_admin_language_landing_page_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/roles/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_admin_management_role_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/management/staff_members/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_admin_management_staff_member_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/reviews/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_admin_review_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/book/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_book_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/leads/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_lead_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/magic_links/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_magic_link_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/passkey_session/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_passkey_session_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/phone_auth/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_phone_auth_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/remind_password/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_remind_password_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/session/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_session_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/users/new
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const new_user_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/api/blog_posts/:id/next(.:format)
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const next_api_blog_post_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter, format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/pages/:id
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const page_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/passkey_session
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const passkey_session_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/password/:token
 * @param {any} token
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const password_path: ((
  token: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/phone_auth
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const phone_auth_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/representations/redirect/:signed_blob_id/:variation_key/*filename(.:format)
 * @param {any} signed_blob_id
 * @param {any} variation_key
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_blob_representation_path: ((
  signed_blob_id: RequiredRouteParameter,
  variation_key: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/representations/proxy/:signed_blob_id/:variation_key/*filename(.:format)
 * @param {any} signed_blob_id
 * @param {any} variation_key
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_blob_representation_proxy_path: ((
  signed_blob_id: RequiredRouteParameter,
  variation_key: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/direct_uploads(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_direct_uploads_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/disk/:encoded_key/*filename(.:format)
 * @param {any} encoded_key
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_disk_service_path: ((
  encoded_key: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /up(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_health_check_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/info(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_info_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/info/notes(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_info_notes_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/info/properties(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_info_properties_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/info/routes(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_info_routes_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/mailers(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_mailers_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/blobs/redirect/:signed_id/*filename(.:format)
 * @param {any} signed_id
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_service_blob_path: ((
  signed_id: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/blobs/proxy/:signed_id/*filename(.:format)
 * @param {any} signed_id
 * @param {any} filename
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const rails_service_blob_proxy_path: ((
  signed_id: RequiredRouteParameter,
  filename: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/blog_posts/:id/related_courses
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const related_courses_admin_blog_post_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/remind_password
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const remind_password_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/languages/:id/review
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const review_admin_language_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/language_lessons/:id/review
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const review_admin_language_lesson_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/reviews
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const reviews_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /(:suffix)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const root_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /ru/languages/layout-designer(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const ru_languages_layout_designer_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /ru/languages/osnovy-python(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const ru_languages_osnovy_python_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /ru/languages/pre-course-java(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const ru_languages_pre_course_java_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /ru/languages/pre-course-javascript(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const ru_languages_pre_course_javascript_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /ru/languages/pre-course-python(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const ru_languages_pre_course_python_path: ((
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /res
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const ruby_event_store_browser_app_path: ((
  options?: RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/admin/api/users/search
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const search_admin_api_users_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/session
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const session_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/languages/:id/success
 * @param {any} id
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const success_language_path: ((
  id: RequiredRouteParameter,
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/locale/switch
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const switch_locale_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * /rails/active_storage/disk/:encoded_token(.:format)
 * @param {any} encoded_token
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const update_rails_disk_service_path: ((
  encoded_token: RequiredRouteParameter,
  options?: {format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/users
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const users_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/phone_auth/verify
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const verify_phone_auth_path: ((
  options?: {suffix?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

/**
 * Generates rails route to
 * (/:suffix)/api/feeds/yandex_courses(.:format)
 * @param {object | undefined} options
 * @returns {string} route path
 */
export const yandex_courses_api_feeds_path: ((
  options?: {suffix?: OptionalRouteParameter, format?: OptionalRouteParameter} & RouteOptions
) => string) & RouteHelperExtras;

