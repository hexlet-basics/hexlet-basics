# Fixtures for `active_storage_blobs` (ActiveStorage blob metadata). The `key`
# is the bucket key the `/storage/{key}` read path serves; the cover URL in the
# read model is built from it. service_name/byte_size/filename are NOT NULL.
- id: 7001
  key: blogcoverkey001
  filename: cover.png
  content_type: image/png
  byte_size: 1234
  service_name: local
  created_at: '2026-07-26 20:50:29'
