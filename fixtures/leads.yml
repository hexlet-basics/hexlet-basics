# Fixtures for `leads`, HAND-CURATED (the legacy Rails fixture set has no leads).
# `make fixtures-import` is non-destructive and skips existing files, so this
# survives re-imports. testfixtures disables referential integrity while loading,
# so `user_id` need not match a users fixture (and full_name is derived from the
# user, which the admin list serializer returns null until the User schema lands).
# Loaded by testfixtures inside each test's rollback transaction.
- id: 1
  user_id: 101
  email: anna@example.com
  phone: '+79990000001'
  whatsapp:
  telegram: '@anna'
  survey_answers_data:
  courses_data:
  state: new
  created_at: '2026-07-26 20:50:29'
  updated_at: '2026-07-26 20:50:29'
- id: 2
  user_id: 102
  email: boris@example.com
  phone:
  whatsapp: '+79990000002'
  telegram:
  survey_answers_data:
  courses_data:
  state: processed
  created_at: '2026-07-26 20:50:30'
  updated_at: '2026-07-26 20:50:30'
