DEFAULTS: &DEFAULTS
  password_digest: <%= BCrypt::Password.create('password') %>
  email: $LABEL@test.io
  state: active

full:
  <<: *DEFAULTS
  admin: true
  reset_password_token: <%= SecureRandom.hex(10) %>

one:
  <<: *DEFAULTS

two:
  <<: *DEFAULTS

admin:
  <<: *DEFAULTS
  admin: true
