// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"hexletbasics/ent/activestorageattachment"
	"hexletbasics/ent/activestorageblob"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ActiveStorageAttachment is the model entity for the ActiveStorageAttachment schema.
type ActiveStorageAttachment struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// RecordID holds the value of the "record_id" field.
	RecordID int `json:"record_id,omitempty"`
	// RecordType holds the value of the "record_type" field.
	RecordType string `json:"record_type,omitempty"`
	// BlobID holds the value of the "blob_id" field.
	BlobID int `json:"blob_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ActiveStorageAttachmentQuery when eager-loading is set.
	Edges        ActiveStorageAttachmentEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ActiveStorageAttachmentEdges holds the relations/edges for other nodes in the graph.
type ActiveStorageAttachmentEdges struct {
	// Blob holds the value of the blob edge.
	Blob *ActiveStorageBlob `json:"blob,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BlobOrErr returns the Blob value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ActiveStorageAttachmentEdges) BlobOrErr() (*ActiveStorageBlob, error) {
	if e.Blob != nil {
		return e.Blob, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: activestorageblob.Label}
	}
	return nil, &NotLoadedError{edge: "blob"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ActiveStorageAttachment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case activestorageattachment.FieldID, activestorageattachment.FieldRecordID, activestorageattachment.FieldBlobID:
			values[i] = new(sql.NullInt64)
		case activestorageattachment.FieldName, activestorageattachment.FieldRecordType:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ActiveStorageAttachment fields.
func (_m *ActiveStorageAttachment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case activestorageattachment.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case activestorageattachment.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case activestorageattachment.FieldRecordID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field record_id", values[i])
			} else if value.Valid {
				_m.RecordID = int(value.Int64)
			}
		case activestorageattachment.FieldRecordType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field record_type", values[i])
			} else if value.Valid {
				_m.RecordType = value.String
			}
		case activestorageattachment.FieldBlobID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field blob_id", values[i])
			} else if value.Valid {
				_m.BlobID = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ActiveStorageAttachment.
// This includes values selected through modifiers, order, etc.
func (_m *ActiveStorageAttachment) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBlob queries the "blob" edge of the ActiveStorageAttachment entity.
func (_m *ActiveStorageAttachment) QueryBlob() *ActiveStorageBlobQuery {
	return NewActiveStorageAttachmentClient(_m.config).QueryBlob(_m)
}

// Update returns a builder for updating this ActiveStorageAttachment.
// Note that you need to call ActiveStorageAttachment.Unwrap() before calling this method if this ActiveStorageAttachment
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ActiveStorageAttachment) Update() *ActiveStorageAttachmentUpdateOne {
	return NewActiveStorageAttachmentClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ActiveStorageAttachment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ActiveStorageAttachment) Unwrap() *ActiveStorageAttachment {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ActiveStorageAttachment is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ActiveStorageAttachment) String() string {
	var builder strings.Builder
	builder.WriteString("ActiveStorageAttachment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("record_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RecordID))
	builder.WriteString(", ")
	builder.WriteString("record_type=")
	builder.WriteString(_m.RecordType)
	builder.WriteString(", ")
	builder.WriteString("blob_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.BlobID))
	builder.WriteByte(')')
	return builder.String()
}

// ActiveStorageAttachments is a parsable slice of ActiveStorageAttachment.
type ActiveStorageAttachments []*ActiveStorageAttachment
