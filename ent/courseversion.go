// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"hexletbasics/ent/courseversion"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CourseVersion is the model entity for the CourseVersion schema.
type CourseVersion struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Result holds the value of the "result" field.
	Result *string `json:"result,omitempty"`
	// State holds the value of the "state" field.
	State *string `json:"state,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CourseVersion) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case courseversion.FieldID:
			values[i] = new(sql.NullInt64)
		case courseversion.FieldResult, courseversion.FieldState:
			values[i] = new(sql.NullString)
		case courseversion.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CourseVersion fields.
func (_m *CourseVersion) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case courseversion.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case courseversion.FieldResult:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field result", values[i])
			} else if value.Valid {
				_m.Result = new(string)
				*_m.Result = value.String
			}
		case courseversion.FieldState:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field state", values[i])
			} else if value.Valid {
				_m.State = new(string)
				*_m.State = value.String
			}
		case courseversion.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CourseVersion.
// This includes values selected through modifiers, order, etc.
func (_m *CourseVersion) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this CourseVersion.
// Note that you need to call CourseVersion.Unwrap() before calling this method if this CourseVersion
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CourseVersion) Update() *CourseVersionUpdateOne {
	return NewCourseVersionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CourseVersion entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CourseVersion) Unwrap() *CourseVersion {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: CourseVersion is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CourseVersion) String() string {
	var builder strings.Builder
	builder.WriteString("CourseVersion(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	if v := _m.Result; v != nil {
		builder.WriteString("result=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := _m.State; v != nil {
		builder.WriteString("state=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CourseVersions is a parsable slice of CourseVersion.
type CourseVersions []*CourseVersion
