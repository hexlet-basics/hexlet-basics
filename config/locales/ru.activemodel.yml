ru:
  activemodel:
    errors:
      messages:
        blank: не может быть пустым
      models:
        sign_in:
          attributes:
            password:
              cannot_sign_in: Неправильные email/пароль
            email:
              user_does_not_exist: не существует
